# 切图大师上线安排

## 已就绪

- 站点文件：`index.html`（单文件静态网页）
- 图片识别、透明背景处理、单图下载和 ZIP 导出均在浏览器本地完成；不需要后端、数据库或 API 密钥。
- 推荐托管：Cloudflare Pages 的 Direct Upload。它适合直接上传当前的静态文件夹。

## 域名候选（均需在购买页进行最终可用性与价格确认）

| 优先级 | 候选 | 说明 |
| --- | --- | --- |
| 1 | `uislicer.app` | 简洁、准确描述 UI 切图工具，适合产品站。 |
| 2 | `slicemaster.app` | 对应英文名 Slice Master，品牌感更强。 |
| 3 | `qietudashi.com` | 与中文名“切图大师”一致，方便中文用户记忆。 |
| 4 | `iconcutter.app` | 突出图标切图用途。 |

建议购买主域名后，同时购买一个容易输入的备用域名，并把备用域名 301 跳转到主域名。域名字符应使用英文、数字和连字符；不要使用中文域名作为主站域名。

## 推荐执行路径

1. 在 Cloudflare 域名搜索页逐一检查候选域名，确认实时价格与可注册状态。
2. 由域名最终所有人填写注册人、账单和付款资料，完成购买并验证注册邮箱。
3. 在 Cloudflare Pages 创建 Direct Upload 项目，上传本目录的站点文件。
4. 先用 `*.pages.dev` 地址验收上传、识别、单图下载、ZIP 下载和中英文切换。
5. 在 Pages 的 Custom domains 中绑定主域名；根域名需要将 DNS 名称服务器交给 Cloudflare 管理。
6. 验证 HTTPS、桌面与手机显示、ZIP 可解压、域名解析和备用域名跳转后公开发布。

## 发布前的最小检查

- 明确域名注册人是个人还是公司；续费与邮箱由实际所有人保管。
- 给公开站点补一页简短的隐私说明：图片只在本地浏览器处理，不会上传或保存。
- 如果目标是中国大陆稳定访问并使用中国大陆服务器/CDN，改用境内云服务并按其要求办理备案；这与当前的 Cloudflare Pages 快速上线方案不同。

## 权限边界

我可以协助检查域名候选、准备部署包、完成 Pages 上传和 DNS 配置验证；域名注册资料和付款必须由域名实际所有人确认并完成。公开发布前会再次请你确认最终域名和上线范围。
